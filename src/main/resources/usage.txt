Usage:

-a, --alias
        Required aliases of users who will eat today
-f, --force
        this will force a change - the current person on duty will be remembered and queue will be changed
-e, --exclude
        aliases to exclude from calculation
-q, --queue
        queue file name to use
-r, --revert
        revert the queue file
-n, --nice
        parses aliases nicely, three per alias even if they happen to kludge together (what happens if copypasted from google doc)